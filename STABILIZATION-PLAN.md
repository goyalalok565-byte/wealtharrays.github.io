# Wealth Arrays Stabilization

The production calculator pages use a single canonical runtime bundle generated from the existing runtime modules. Existing module source files remain available for maintenance; calculator HTML pages do not load them individually.

The stabilization build also keeps local dependency directories out of the deployable repository.
