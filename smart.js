/* Smart Calculator Assistant removed from production.
   The feature was not reliable enough for a financial product.
   Calculator results remain deterministic and local; no assistant UI is injected. */
