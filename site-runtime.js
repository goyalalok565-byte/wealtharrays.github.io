/* Wealth Arrays visual runtime — cards, charts and defensive UX fallbacks. */
(function () {
  'use strict';
  function resultRows() { return Array.from(document.querySelectorAll('.calc-result-row')).map(function (row) { var text = row.querySelector('.calc-result-value')?.textContent || ''; return { label: row.querySelector('.calc-result-label')?.textContent?.trim() || '', text: text.trim(), value: Number(text.replace(/[^0-9.eE+-]/g, '')) }; }).filter(function (row) { return Number.isFinite(row.value); }); }
  function safePie(host) { var rows = resultRows().filter(function (row) { return row.value > 0; }).slice(0, 6); if (!rows.length) return; var total = rows.reduce(function (a, r) { return a + r.value; }, 0); if (!Number.isFinite(total) || total <= 0) return; var ns = 'http://www.w3.org/2000/svg', svg = document.createElementNS(ns, 'svg'); svg.setAttribute('viewBox', '0 0 200 200'); svg.setAttribute('role', 'img'); svg.setAttribute('aria-label', 'Result composition chart'); var angle = -Math.PI / 2; rows.forEach(function (r) { var next = angle + (r.value / total) * Math.PI * 2; var x1 = 100 + 78 * Math.cos(angle), y1 = 100 + 78 * Math.sin(angle), x2 = 100 + 78 * Math.cos(next), y2 = 100 + 78 * Math.sin(next), large = next - angle > Math.PI ? 1 : 0; var path = document.createElementNS(ns, 'path'); path.setAttribute('d', 'M100 100 L' + x1 + ' ' + y1 + ' A78 78 0 ' + large + ' 1 ' + x2 + ' ' + y2 + ' Z'); path.setAttribute('fill', 'none'); path.setAttribute('stroke', 'currentColor'); path.setAttribute('stroke-width', '30'); path.setAttribute('stroke-dasharray', '1 1'); path.setAttribute('pathLength', '1'); path.setAttribute('stroke-dashoffset', String(-angle / (Math.PI * 2))); svg.appendChild(path); angle = next; }); host.innerHTML = ''; host.appendChild(svg); }
  function graphGuard() { document.querySelectorAll('[data-chart],.calc-chart,.chart-wrap').forEach(function (host) { var svg = host.querySelector('svg'); if (!svg) return; var rect = svg.getBoundingClientRect(); if (rect.width >= 20 && rect.height >= 20 && !/NaN|Infinity|-Infinity/.test(svg.outerHTML)) return; safePie(host); }); }
  function styles() { if (document.getElementById('wa-visual-runtime-css')) return; var style = document.createElement('style'); style.id = 'wa-visual-runtime-css'; style.textContent = '.tool-card[data-wa-category],.home-tool-card[data-wa-category],.ledger-row[data-wa-category]{position:relative!important;box-sizing:border-box!important;background:var(--surface)!important;border-left:1.5px solid var(--line)!important;border-right:1.5px solid var(--line)!important;border-bottom:1.5px solid var(--line)!important;border-top:3.5px solid var(--wa-card-accent)!important;border-radius:18px!important;box-shadow:0 6px 20px rgba(16,24,40,.06)!important;overflow:hidden}.wa-scale-safe{margin-top:14px;border:1px solid var(--line);border-radius:15px;padding:16px;background:var(--surface2)}.wa-scale-safe-head span{font-size:9px;letter-spacing:.14em;color:var(--accent);font-weight:850}.wa-scale-safe-head h3{margin:3px 0;font-size:15px}.wa-scale-safe-head p{margin:0 0 12px;color:var(--muted);font-size:10px}.wa-pie-wrap{display:grid;grid-template-columns:160px 1fr;gap:18px;align-items:center}.wa-pie-wrap svg{width:160px;height:160px;display:block}.wa-pie-row{display:grid;grid-template-columns:10px 1fr auto;gap:7px;align-items:center;padding:8px 0;border-top:1px solid var(--line);font-size:10px}.wa-pie-row i{width:9px;height:9px;border-radius:50%;display:block}@media(max-width:600px){.wa-pie-wrap{grid-template-columns:1fr}.wa-pie-wrap svg{margin:auto}}.wa-search-fallback{position:absolute!important;left:0!important;right:0!important;top:calc(100% + 8px)!important;z-index:2147483646!important;background:var(--surface,#fff)!important;border:1px solid var(--line,#e2e6ed)!important;border-radius:14px!important;box-shadow:0 18px 50px rgba(0,0,0,.16)!important;overflow:hidden!important}.wa-search-fallback a{display:flex!important;justify-content:space-between!important;gap:12px!important;padding:13px 15px!important;text-decoration:none!important;color:inherit!important;border-bottom:1px solid var(--line,#e2e6ed)!important}.wa-search-fallback a:last-child{border-bottom:0!important}.wa-search-fallback span{display:grid!important;gap:2px!important}.wa-search-fallback small{color:var(--muted,#667085)!important;font-size:10px!important}'; document.head.appendChild(style); }
  function installController() {
    var deferred = null;
    window.addEventListener('beforeinstallprompt', function (event) { event.preventDefault(); deferred = event; window.__WA_INSTALL_PROMPT__ = event; document.querySelectorAll('[data-wa-install-action="install"]').forEach(function (button) { button.hidden = false; button.disabled = false; button.removeAttribute('aria-hidden'); }); });
    window.addEventListener('appinstalled', function () { deferred = null; window.__WA_INSTALL_PROMPT__ = null; document.querySelectorAll('[data-wa-install-action="install"]').forEach(function (button) { button.hidden = true; button.disabled = true; button.setAttribute('aria-hidden','true'); }); });
    document.addEventListener('click', function (event) { var button = event.target.closest && event.target.closest('[data-wa-install-action="install"]'); if (!button) return; if (!deferred) { button.hidden = true; button.disabled = true; button.setAttribute('aria-hidden','true'); return; } event.preventDefault(); var promptEvent = deferred; deferred = null; window.__WA_INSTALL_PROMPT__ = null; promptEvent.prompt(); promptEvent.userChoice.catch(function () {}).finally(function () { if (!window.matchMedia('(display-mode: standalone)').matches) { button.hidden = true; button.disabled = true; button.setAttribute('aria-hidden','true'); } }); }, true);
    setTimeout(function () { if (window.matchMedia('(display-mode: standalone)').matches || window.navigator.standalone === true) document.querySelectorAll('[data-wa-install-action="install"]').forEach(function (button) { button.hidden = true; button.disabled = true; button.setAttribute('aria-hidden','true'); }); }, 800);
  }
  function init() { styles(); graphGuard(); installController(); setInterval(graphGuard, 800); }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init, { once: true }); else init();
})();
