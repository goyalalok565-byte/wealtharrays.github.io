name: Phase 4 Premium Hardening

# Retired automatic HTML mutator. AdSense/CMP/ads.txt configuration is now source-controlled.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Production source changes are validated by the stabilization pipeline."
