name: Phase 3 Static SEO Build

# Retired as an automatic HTML mutator. SEO source changes remain explicit and are validated by CI.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. SEO changes are source-controlled and validated before deployment."
