name: Global Theme Runtime Injection

# Retired: deterministic stabilization owns production HTML. Kept as a manual no-op record so historical workflow links remain valid.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Use stabilization-build.yml for production HTML changes."
