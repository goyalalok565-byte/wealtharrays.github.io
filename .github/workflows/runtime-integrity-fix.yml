name: Runtime integrity fix

# Retired: integrity corrections must be made in source and validated by CI, never rewritten after every push.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Use source-controlled fixes plus stabilization validation."
